// Sentry client config — only loads when @sentry/nextjs is installed and SENTRY_DSN is set.
// Install: npm install @sentry/nextjs
// Then add SENTRY_DSN to your .env.local
//
// This file is intentionally minimal to not break builds without Sentry.
export {};
